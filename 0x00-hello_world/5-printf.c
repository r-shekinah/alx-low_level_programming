#include <stdio.h>
int main/** main - A program that prints a line with printf function
	  * return : 0 (success)
	  */(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
