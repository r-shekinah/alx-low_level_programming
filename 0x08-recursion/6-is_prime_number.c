#include "main.h"
int is_prime_number/**
		     * is_prime_number - checks if input is a prime number
		     * @n : input
		     * return : 1 for prime, else 0
		     */(int n)
{
	int y = 0;
	y = y + 1;

	if (n % y = 0)
		return(0);

